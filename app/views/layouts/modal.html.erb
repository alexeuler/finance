<div class="modal-container">
  <% if content_for?(:modal_header) %>
      <div class="header">
        <%= yield :modal_header %>
      </div>
  <% end %>

  <div class="body">
    <div class="errors"></div>
    <%= yield %>
  </div>

</div>